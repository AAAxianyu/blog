name: Deploy to Server

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: production
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install, test and build
        env:
          DEPLOYMENT_VERSION: ${{ github.sha }}
        run: |
          npm ci
          npm run lint
          npm run test
          npm run build

      - name: Create release archive
        run: |
          set -euo pipefail
          mkdir -p release/.next release/seed-posts release/ops
          cp -a .next/standalone/. release/
          cp -a .next/static release/.next/static
          cp -a public release/public
          cp -a content/posts/. release/seed-posts/
          cp ops/x1anyu-blog.service release/ops/
          cp nginx.conf release/ops/
          tar -C release -czf "blog-${GITHUB_SHA}.tgz" .
          split --numeric-suffixes=0 --suffix-length=2 --number=l/4 \
            "blog-${GITHUB_SHA}.tgz" "blog-${GITHUB_SHA}.part-"

      - name: Configure SSH
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq sshpass
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          ssh-keyscan -H "$SSH_HOST" >> ~/.ssh/known_hosts

      - name: Upload release
        env:
          SSHPASS: ${{ secrets.SSH_PASSWORD }}
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USERNAME: ${{ secrets.SSH_USERNAME }}
        run: |
          pids=()
          for part in "blog-${GITHUB_SHA}".part-*; do
            sshpass -e scp "$part" "$SSH_USERNAME@$SSH_HOST:/tmp/$part" &
            pids+=("$!")
          done
          for pid in "${pids[@]}"; do
            wait "$pid"
          done

      - name: Activate release with rollback
        env:
          SSHPASS: ${{ secrets.SSH_PASSWORD }}
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USERNAME: ${{ secrets.SSH_USERNAME }}
        run: |
          sshpass -e ssh "$SSH_USERNAME@$SSH_HOST" \
            "cat /tmp/blog-${GITHUB_SHA}.part-* > /tmp/blog-${GITHUB_SHA}.tgz && \
             rm -f /tmp/blog-${GITHUB_SHA}.part-* && \
             bash -s -- '${GITHUB_SHA}'" < ops/activate-release.sh

      - name: Verify public endpoint
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
        run: |
          curl --fail-with-body --silent --show-error --retry 5 --retry-delay 2 \
            --resolve "x1anyu.top:443:${SSH_HOST}" \
            "https://x1anyu.top/api/health"
